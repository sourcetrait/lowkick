[package]
name = "sdl_shim"
version = "0.0.0-1"
edition = "2021"
description = "A preload library that logs the SDL calls QEMU's window makes, with timestamps and callers, for the jab probe."
license = "AGPL-3.0-only"
publish = false

[lib]
crate-type = ["cdylib"]

[dependencies]
